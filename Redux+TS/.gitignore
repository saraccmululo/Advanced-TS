# dependencies
/node_modules

# build outputs
/dist
/.vite

# local env files
.env
.env.*.local

# IDE settings
.vscode
.idea

# Mac system files
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Optional: TypeScript build cache
*.tsbuildinfo

# Optional: coverage reports
coverage

# Optional: local dev server SSL keys
*.pem

# Optional: local editor swap/backup files
*.swp
*.swo

# Optional: Mac + Linux Trash
.Trashes
*.trash

# Optional: local VS Code settings
.vscode/

# Optional: local eslint cache
.eslintcache

# Optional: parcel cache (if using)
.cache

# Optional: yarn v2+
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Optional: next/nuxt builds if accidentally added
.next
.nuxt

# Optional: vercel/Netlify config outputs if accidentally added
.vercel
.netlify

# Optional: SvelteKit build cache if you experiment
.svelte-kit
